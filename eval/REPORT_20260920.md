# 2026-09-20 真实模型评测与可用性改进

模型：sensenova-6.8-flash-lite。此次使用真实 API。

## 完整运行

原始记录：`runs/20260920-150547-711718/results.json`。

- 规则通过 7/8；最终回答 8/8。
- 27 次模型调用；prompt 33,570 tokens，completion 4,423，总计 37,993。
- Provider / 配置 / 执行器失败均为 0；步数耗尽 0；重复调用拦截 0。
- 唯一失败 task_3：只列顶层，遗漏 notes 下三个文件。
- 原始报告把遗漏归为 Test Design；分类代码随后已改成 Model Behavior，原始证据不改写。

人工复核：三次摘要均覆盖原文核心概念，写入产物保存在 output_content。
双文件比较主要有据，但“都有代码示例”的表述与后文“只有流程图”不一致。
缺失文件题正确报告不存在；指定内容写入逐字一致；推荐题的工具链确认读取了全部六个文件。
推荐题说 v2 最精简的表述不能仅靠文件大小认定，需要明确简洁性的语义标准。

本轮源工作区已有此前生成的 agent_summary_eval.md，写摘要属于覆盖已有文件。
输入与早期基线不同，不能从 46,011 降到 37,993 就断言压缩带来固定比例收益。
目前仍未覆盖长任务上下文质量、跨轮恢复等场景。

## 修复后定向回归

提示词明确 list_files 只列一层，列工作目录应继续查看子目录。
同时明确未读取文件不能断言内容。
记录：`runs/20260920-151132-519480/results.json`。

- 目录题 1/1 通过；3 次调用，3,185 tokens。
- 实际调用仅 list_files 根目录和 notes；最终回答包含全部六个文件。
- 修复后未重新运行全部八题，不能将两次运行拼成“新版本 8/8”。

## 工程交付

- Eval 每题临时副本隔离，不再删除或还原真实工作目录。
- 每题完成后原子保存独立运行记录；保留写入文本供复核。
- 客户端配置失败返回结构化结果；900 秒子进程超时记录失败后继续。
- 有失败则命令退出码非零；配置与 Provider 故障分别统计。
- 新增 cli.py 单任务 JSON 入口，区分完成、未完成、失败、取消。
- 通过：19 项原有指标检查、5 项新评测回归、4 项 CLI 检查，以及循环/沙盒检查。

连接环境：127.0.0.1:7897 未监听，SOCKS 依赖安装未成功。
本轮仅在进程内对 token.sensenova.cn 设置 NO_PROXY 并清空 ALL_PROXY；未修改全局代理或 .env。
早期失败、中断和短探测不计入上面的完成运行 token 总数，不能以它估算整段操作账单。

尚需完成：会话保存/恢复、长文件分段读取和重读、写入保护、更多真实场景验收。
当前仍定位于本地文件助手，尚未达到通用完整 Agent 的验收标准。
